streamlit>=1.50
pandas>=2.0
openpyxl>=3.1
# El Excel de salida se genera con xlsxwriter: openpyxl arma el libro entero
# en memoria y es lo que tumbaba el proceso con el catalogo completo.
xlsxwriter>=3.1
xlrd>=2.0.1
google-cloud-bigquery>=3.20
google-auth>=2.29
db-dtypes>=1.2
pyarrow>=14.0
# Subida de videos al bucket de imagenes (Mantenedor de Videos).
# La app siempre LEYO del bucket; escribir en el es nuevo.
boto3>=1.34
