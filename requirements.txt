# 1.50 es el minimo REAL: es la primera version cuya `width=` aceptan
# `dataframe`, `button` y `download_button`. `use_container_width` quedaba
# obsoleto el 2025-12-31 y la app lo usaba 105 veces: el dia que Streamlit lo
# quite, cada una de esas llamadas revienta y no queda ni una pantalla en pie.
streamlit>=1.50
pandas>=2.0
openpyxl>=3.1
# El Excel de salida se genera con xlsxwriter: openpyxl arma el libro entero
# en memoria y es lo que tumbaba el proceso con el catalogo completo.
xlsxwriter>=3.1
xlrd>=2.0.1
google-cloud-bigquery>=3.20
google-auth>=2.29
db-dtypes>=1.2
# Sin este paquete, `to_dataframe()` baja el resultado de BigQuery por el
# endpoint REST y la libreria lo avisa en CADA consulta -- eso es lo que
# llenaba el log de Streamlit Cloud. Con el, el maestro ARTI (653.000 filas)
# y el stock vienen por la Storage API, en Arrow y en streaming.
#
# Es wheel puro (py3-none-any) y su `grpcio` tiene wheel manylinux x86_64 para
# cp314, que es el Python de Streamlit Cloud: comprobado, no hay que compilar.
#
# La Storage API pide `bigquery.readsessions.create`, que el REST no pide. Si
# la cuenta de servicio no lo tiene, `bigquery_a_dataframe` reintenta por REST:
# tener el paquete nunca deja la app peor que sin el.
google-cloud-bigquery-storage>=2.25
pyarrow>=14.0
# Se usa para reducir los logos antes de embutirlos como data URI en el HTML.
# Llega por Streamlit, pero aqui se importa directo, asi que va declarada.
pillow>=10.0
